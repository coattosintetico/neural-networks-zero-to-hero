uv run --with jupyter jupyter lab --no-browser

# there's another option which seems to run jupyter **as a standalone tool**--we
# don't want it, we want the jupyter lab in the same environment as this project
# uv tool run jupyter lab
